<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/blue" /> <!-- Background color -->
    <corners android:radius="8dp" /> <!-- Corner radius -->
    <stroke
        android:width="2dp"
        android:color="@color/light_blue" /> <!-- Border width and color -->
</shape>
