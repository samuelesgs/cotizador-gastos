<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/card_color" /> <!-- Background color -->
    <corners android:radius="8dp" /> <!-- Corner radius -->
    <stroke
        android:width="2dp"
        android:color="@color/card_border_color" /> <!-- Border width and color -->
</shape>
